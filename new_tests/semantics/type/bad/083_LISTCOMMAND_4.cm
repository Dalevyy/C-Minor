/*
    Type Error 452

    1| append(a,6)
    List 'a' has type List[List[Int]], but the final argument for the append list command has type Int.

    Suggestion:
    To use the append list command on 'a', the final argument must be of type 'List[List[Int]]' or 'List[Int]'.
*/

def a:List[List[Int]] = List(List(1,2,3),List(4,5),List(6,7,8,9))

append(a,6)
