/*
    Operator Overload Test #1

    This tests if a simple unary operator 
    overload can be executed.

    Output:
        Value of x = false 
        Value of x = true
*/

class A {
    protected x:Bool

    public operator not() => Void {
        set x = not x 
    }

    public method print() => Void {
        cout << 'Value of x = ' << x << endl 
    }
}

def a:A = new A(x=False)
a.print()
not a 
a.print()
