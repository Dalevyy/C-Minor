/*
    Class Template Runtime Test #6

    This tests if the correct method will be
    called when instantiating a class template. 

    Output:
        Specific Value is 5 
*/

class A<discr T> {
    public method print(in a:T) => Void {
        cout << 'Non-Specific Value is ' << a 
    }

    public method print(in a:Int) => Void {
        cout << 'Specific Value is ' << a 
    }
}

def a:A<Int> = new A<Int>()
a.print(5)
