/*
    Class Template Runtime Test #4

    This tests if a class can be instantiated with
    the property modifier.

    Output:
        o
        e
*/

class A<discr T> {
    property x:T 
}

def a:A<Char> = new A<Char>(x='o')

cout << a.get_x()
a.set_x('e')
cout << a.get_x()
