/*
    Binary Expression Test #6

    This tests if the 'as?' operator works.
*/

class A { public x:Int }
class B inherits A { public y:Int }

def a:A = new A(x=5)

if(a as? B) {
    cout << 'Yes'
} else {
    cout << 'No'
}
