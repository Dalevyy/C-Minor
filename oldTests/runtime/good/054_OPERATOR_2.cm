/*
    Operator Overloading Test #2

    This tests if a unary operator overload
    can work on a subtype object.

    Output:
        Value of x = false 
        Value of x = true 
*/

class A {
    protected x:Bool

    public operator not() => Void {
        set x = not x 
    }

    public method print() => Void {
        cout << 'Value of x = ' << x << endl 
    }
}

class B inherits A { }

def b:B = new B(x=False)
b.print()
not b 
b.print()
